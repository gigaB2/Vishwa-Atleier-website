### Task 1: HTML Structure, Book Frame & Parchment CSS Design System

**Files:**
- Create: `To-do.html`

**Interfaces:**
- Produces: Base HTML DOM layout with `.book-container`, `.book-wrapper`, `.page.left-page`, `.page.right-page`, header date navigation controls, and CSS variables for the book theme.

**Task Instructions & Code:**
1. Create `To-do.html` at workspace root `C:\Users\Admin\Desktop\work\main\To-do.html`.
2. Include Google Fonts (Outfit & Caveat).
3. Set CSS variables for:
   - `--bg-dark`: `#121519`
   - `--cover-leather`: `#231c18`
   - `--paper-cream`: `#faf7f0`
   - `--text-main`: `#2c2925`
   - `--accent-brown`: `#8b5a2b`
4. Build responsive 2-page book spread (`.pages-spread`, `.left-page`, `.right-page`).
5. Include Top Navigation controls:
   - Prev Day button (`#prev-date-btn`)
   - Date picker input (`#date-picker`)
   - Formatted Date title (`#date-formatted-title`)
   - Today button (`#today-btn`)
   - Next Day button (`#next-date-btn`)
6. Include spine shadow (`.book-spine-line`) and leather bookmark ribbon (`.bookmark-ribbon`).
7. Include Left Page header (`Daily Action Tasks`, `#task-counter-badge`, `#tasks-container`).
8. Include Right Page header (`Thoughts & Notes`, `#save-status-badge`, `#notes-container`).
9. Commit with message: `feat: initial HTML structure and hardcover book CSS design system`.
